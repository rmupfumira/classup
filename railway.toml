[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
startCommand = "python scripts/startup.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
